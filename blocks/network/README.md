# network

Displays:

* Wifi signal strength
* ESSID
* Private IP
* Public IP

Displaying the public IP relies sending an HTTP GET request to $IP_ENDPOINT and
expects the IP address as a plaintext response.

## Configs

See "env.example" for instructions.

